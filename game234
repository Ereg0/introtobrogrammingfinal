import pygame
import sys
# importing pygame and system specific parameters
pygame.init()

WINDOW_WIDTH = 1200
WINDOW_HEIGHT = 600
# the size of the gaming screen
FPS = 20
# frames per second
BLACK = (0, 0, 0)
GREEN = (0, 255, 0)
# colors of screen rgbvalues
cactus = pygame.image.load('cactus.png')
# image from files
cactus = cactus.get_rect()
cactus.left = 0
# the cactus image in super mario to be imported and implimented on the screen
fire_img = pygame.image.load('fire.png')
firecactus = fire_img.get_rect()
# importing fire image 
